# Copy to .env and fill in. .env is gitignored — this template is the only one committed.

HOSPITABLE_API_TOKEN=
HOSPITABLE_API_BASE=https://public.api.hospitable.com/v2

# PriceLabs free tier = MCP server only (client ID); REST API is a paid add-on
PRICELABS_CLIENT_ID=
PRICELABS_API_BASE=https://api.pricelabs.co/v1
PRICELABS_MCP_URL=https://mcp.pricelabs.co/mcp
